const codeOffer53_1 = `
/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */

function search(nums, target) {
  return nums.filter(item => item === target).length;
};
`

module.exports = {
  codeOffer53_1
}